# ANGULAR :- 
    - Angular is a type script based frontend framework developed by google.
    - it helps us build a single page application. 
    - SPA : Single Page application 
        - where the page does not fully reload when navigation between different views.
        - Example : Youtube
        
    TOOLS FOR ANGULAR 
        - nodeJS : JS runtime envrionment 
        - npm : node package manager
        - angular CLI (Command line Interface) 
            - It is used for create and manage angular app.


# STEPS to create and run angular application - 
1. npm install -g @angular/cli
2. ng new my-First-App
3. ng serve


# File Names and their purpose :
    - package.json - packages list includes depenedancies, den-depenedancies
        - Dependancies - used for Production build
        - dev Dependancies - local depenedancies.

    - style.css - globally changes style
    - .gitignore - files or folders execute 
    - tsconfi.json - typscript configuration file
    - index.html - main html file loads angular 
    - app.config.ts - app configuration file
    - main.ts - entry point of the application


# Component - 
    - is a UI block or structure, 
    - when we create Component 4 files are created
        - html
        - css
        - typescript
        - spec.ts

    - cmd : ng g c componentName


# Data Binding : 
    - connect UI to code 
    - There are 2 types of data Binding
        1. One way 
            - interpolation
                Show data from ts in html
                {{value}}
                
            - Property Binding
                Bind HTML Property to TS variable
                [Property]=value
            - Event Binding
                call function on event
                (click)=()
        2. Two way 
            - ng module
                Sync data both ways - Ts to html and html to ts
                [(ngModel)]="value"




# Interview Questions -
    1. How we create angular project ?
    2. what is meant by ng ? 

Before angular 15 we use or need ng model but we are using angular 21+ version modules are optionals
you can now use standalone components

Directives :
    Think the Directives like magic wands that can change your web page without rewriting it 
    there are 2 types of Directives -Structural and atrributes

    Structural Directives : change your structure of our page 
    ng if ,ng for, ng switch

    atrributes : ng style how it looks 

    Home activity ::
    1. how we can use if else for loop and switch in angular with examples
    

    Routing :: Navigation between  one page to another is Routing


 must use formModule in import while working with if,if-else,switch
 for commonmodule

 * services and Dependancies injection

 a services is like a helper class 
 also used to store data or share logic that many components can use
 instead of writing the same code in every component ,we write it once in a service .

ng on init is lifecycle hook this cycle runs automatically when components load 

activity-1 write a two ex with services in angular and learn more about dependencies injection
activity-2 